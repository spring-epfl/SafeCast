[package]
name = "auth-service"
version = "0.1.0"
edition = "2024"
description = "Authentication Service: credential registry for MLS-SRTP"

[dependencies]
actix-web = { workspace = true }
serde = { workspace = true }
